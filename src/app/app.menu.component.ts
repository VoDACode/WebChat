import {Component} from '@angular/core';

@Component({
  selector: 'app-menu-source',
  templateUrl: 'source/html/menu.components.html',
})

export class AppMenuComponent {
  detailInfoAboutStorageView: boolean;
}
